import numpy as np
num_arr = np.array([20,50,40,5,9])
print(num_arr.dtype) # Data-type of the array’s elements
print(num_arr.shape) # Tuple of array dimensions
print(num_arr.ndim) # array's dimention
print(num_arr)